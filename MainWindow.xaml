<Window x:Class="exui_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:exui_wpf"
        mc:Ignorable="d"
        Title="exui Dashboard Monitor" Height="550" Width="950" Background="#111112">
    <Window.Resources>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" BorderThickness="0,0,0,2" Padding="20,10" Margin="0,0,10,0">
                            <ContentPresenter x:Name="ContentSite" ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#64D2FF"/>
                                <Setter Property="TextBlock.Foreground" Value="#64D2FF"/>
                                <Setter Property="TextBlock.FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="TextBlock.Foreground" Value="#8E8E93"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="5,0,5,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="EXUI CONTROL PANEL" FontSize="14" FontWeight="Bold" Foreground="#8E8E93" VerticalAlignment="Center"/>

            <Border Grid.Column="1" Padding="12,5" CornerRadius="4" Background="#1C1C1E" BorderBrush="#2C2C2E" BorderThickness="1">
                <TextBlock Text="{Binding ConnectionStatus}" FontWeight="Bold" FontSize="11" FontFamily="Consolas">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF453A"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ConnectionStatus}" Value="CONNECTED">
                                    <Setter Property="Foreground" Value="#30D158"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>
        </Grid>

        <TabControl Grid.Row="1" Margin="0,10,0,0">
            <TabItem Header="Templates">
                <Grid Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Margin="0,0,0,15" Background="#1C1C1E" BorderBrush="#2C2C2E" BorderThickness="1" CornerRadius="6" Padding="15,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="CANVAS EDITOR MODE LAYER" Foreground="#64D2FF" FontSize="11" FontWeight="Bold"/>
                                <TextBlock Text="Enable to resize and drag overlays. Disable to trigger complete click-through HUD utility." Foreground="#8E8E93" FontSize="11" Margin="0,3,0,0"/>
                            </StackPanel>

                            <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding ProgramState.IsEditorMode, Mode=TwoWay}">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="LayoutTransform">
                                            <Setter.Value>
                                                <ScaleTransform ScaleX="1.3" ScaleY="1.3"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1">
                        <TextBlock Text="No templates found in the target directory." 
                                Foreground="#8E8E93" 
                                FontSize="14" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Top" 
                                Margin="0,40,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Templates.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Templates}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,10" Background="#1C1C1E" BorderBrush="#3A3A3C" BorderThickness="1" CornerRadius="6" Padding="20,15">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" Foreground="#FFFFFF" FontSize="16" FontWeight="SemiBold"/>
                                                    <TextBlock Text="External compiled dynamic layout module" Foreground="#8E8E93" FontSize="12" Margin="0,4,0,0"/>
                                                </StackPanel>

                                                <CheckBox Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding IsActive, Mode=TwoWay}" Checked="OnTemplateActivationChanged" Unchecked="OnTemplateActivationChanged">
                                                    <CheckBox.Style>
                                                        <Style TargetType="CheckBox">
                                                            <Setter Property="LayoutTransform">
                                                                <Setter.Value>
                                                                    <ScaleTransform ScaleX="1.3" ScaleY="1.3"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </CheckBox.Style>
                                                </CheckBox>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </TabItem>



            <TabItem Header="Debug">
                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*"/>
                        <ColumnDefinition Width="5*"/>
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,10,0" Background="#1C1C1E" BorderBrush="#3A3A3C" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="LIVE DICTIONARY MATRIX" Foreground="#8E8E93" FontWeight="Bold" FontSize="10" Margin="15,12,15,5"/>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="5">
                                <ItemsControl ItemsSource="{Binding TelemetryRows}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="#2C2C2E" BorderThickness="0,0,0,1" Padding="12,8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBox Text="{Binding Key, Mode=OneWay}" IsReadOnly="True" Background="Transparent" BorderThickness="0" SelectionBrush="#64D2FF" Foreground="#AEAEB2" FontSize="13" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                    <TextBox Grid.Column="1" Text="{Binding Value, Mode=OneWay}" IsReadOnly="True" Background="Transparent" BorderThickness="0" SelectionBrush="#64D2FF" Foreground="#64D2FF" FontSize="13" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Margin="10,0,0,0" Background="#1C1C1E" BorderBrush="#3A3A3C" BorderThickness="1" CornerRadius="6" Padding="15,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="NETWORK ENGINE EVENT SUBSYSTEM" Foreground="#8E8E93" FontWeight="Bold" FontSize="10" Margin="0,2,0,10"/>
                            <TextBox Grid.Row="1" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True" Background="Transparent" BorderThickness="0" SelectionBrush="#64D2FF" Foreground="#30D158" FontSize="12" FontFamily="Consolas" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>