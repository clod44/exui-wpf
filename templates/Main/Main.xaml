<Window x:Class="exui_wpf.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="exui Template Control Center" Height="450" Width="450"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen"
        Topmost="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Detected Overlay Templates" FontSize="20" FontWeight="Bold" Foreground="#4FC1FF" Margin="0,0,0,5"/>

        <CheckBox Grid.Row="1" Content="📐 Enable Design Mode (Unlock Overlay Dragging/Scaling)" 
                  IsChecked="True" FontSize="13" Foreground="#858585" Margin="0,0,0,15"
                  Checked="DesignMode_Checked" Unchecked="DesignMode_Unchecked"/>
        
        <ListBox x:Name="TemplateListBox" Grid.Row="2" Background="#252526" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12,10" Width="385" CornerRadius="5" Margin="0,3">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#2D2D30"/>
                                <Setter Property="BorderBrush" Value="#3F3F46"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                        <Setter Property="Background" Value="#0F2D19"/>
                                        <Setter Property="BorderBrush" Value="#00FF00"/>
                                        <Setter Property="BorderThickness" Value="1.5"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{Binding Name}" FontSize="16" FontFamily="Consolas" VerticalAlignment="Center" Foreground="#DCDCDC"/>
                            
                            <Button Grid.Column="1" Content="Launch" Width="75" Height="28" Margin="5,0" Click="Launch_Click" Background="#0E639C" Foreground="White" BorderThickness="0"/>
                            <Button Grid.Column="2" Content="Kill" Width="75" Height="28" Click="Kill_Click" Background="#A1260D" Foreground="White" BorderThickness="0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>